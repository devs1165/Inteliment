steps:

1.extract/unzip the folder
2.go to the folder and open cmd(shift+right click) or terminal(right click)

3.write 'npm install'in terminal or cmd and hit enter.

4.after installing the packages type "node ." TO run run the project(in terminal or cmd).

5.go to the browswee(chrome is more preferable) and type "localhost:9000" to run the project.
  

